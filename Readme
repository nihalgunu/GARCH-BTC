Python 3.10
activate a venv and pip install -r "requirements.txt"

pip freeze > requirements.txt to update dependencies (PLEASE DO THIS BEFORE YOU COMMIT IF YOU ARE ADDING IMPORTS)

python main.py should run all files when complete

data should hold our data (probably grab from yahoo finance or such) scripts are sketelon but shown in the scripts section and we can create evaluation scripts in eval
